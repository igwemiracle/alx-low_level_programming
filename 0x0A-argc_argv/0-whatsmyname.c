#include <stdio.h>
#include "main.h"

/**
 * main - A program the prints its name -
 * @argc: argument count
 * @argv: array pointing to arguements
 * Return: 0 on success
 */

int main(int argc, char *argv[])

{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
